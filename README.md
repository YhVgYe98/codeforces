# codeforces

```shell
make new Q=XXX # create src template
# (code)
# (copy test input to XXX_in.txt)
make Q=XXX # compile and run with sanitizer, then look in XXX_out.txt
# or debug via vscode alternatively
```